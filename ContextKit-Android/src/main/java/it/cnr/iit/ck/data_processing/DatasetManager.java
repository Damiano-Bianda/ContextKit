package it.cnr.iit.ck.data_processing;

import android.content.Context;

import java.io.IOException;
import java.io.StringReader;

import it.cnr.iit.R;
import it.cnr.iit.ck.commons.Utils;

public class DatasetManager {

    /*public static Instances load(int resourceId, Context context) throws IOException {
        String rawResource = Utils.readTextFile(context.getResources().openRawResource(resourceId));
        StringReader datafile = new StringReader(rawResource);
        Instances data = new Instances(datafile);
        return data;
    }*/
}
